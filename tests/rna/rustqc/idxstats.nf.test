nextflow_process {

    name "Test RustQC idxstats output"
    script "../../../modules/local/rustqc_rna.nf"
    process "RUSTQC_RNA"

    tag "rustqc"
    tag "rna"
    tag "idxstats"
    tag "small"

    test("idxstats - regression") {

        tag "regression"

        when {
            params {
                skip_dup_check = true
            }
            process {
                """
                input[0] = Channel.of([
                    [ id: 'test', single_end: false, strandedness: 'reverse' ],
                    file("${projectDir}/test-data/rna/small/test.bam", checkIfExists: true),
                    file("${projectDir}/test-data/rna/small/test.bam.bai", checkIfExists: true)
                ])
                input[1] = file("${projectDir}/test-data/rna/small/chr6.gtf.gz", checkIfExists: true)
                """
            }
        }

        then {
            assert process.success

            def rustqcDir = process.out.results[0][1]
            def actualFile = "${rustqcDir}/samtools/test.idxstats"
            assert path(actualFile).exists() : "idxstats not found in: ${rustqcDir}"

            snapshot(
                path(actualFile).readLines()
            ).match("idxstats_regression")
        }
    }

    test("idxstats - crosscheck vs upstream") {

        tag "crosscheck"

        when {
            params {
                skip_dup_check = true
            }
            process {
                """
                input[0] = Channel.of([
                    [ id: 'test', single_end: false, strandedness: 'reverse' ],
                    file("${projectDir}/test-data/rna/small/test.bam", checkIfExists: true),
                    file("${projectDir}/test-data/rna/small/test.bam.bai", checkIfExists: true)
                ])
                input[1] = file("${projectDir}/test-data/rna/small/chr6.gtf.gz", checkIfExists: true)
                """
            }
        }

        then {
            assert process.success

            def rustqcDir = process.out.results[0][1]
            def actualFile = "${rustqcDir}/samtools/test.idxstats"
            assert path(actualFile).exists() : "idxstats not found in: ${rustqcDir}"

            def upstreamFile = "${projectDir}/snapshots/rna/small/samtools/test.idxstats"

            // idxstats output should be identical between RustQC and samtools
            CompareUtils.textMatch(
                path(actualFile).readLines(),
                path(upstreamFile).readLines(),
                []  // no prefixes to skip
            )
        }
    }
}
