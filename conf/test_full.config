/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running full-size tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Default full test profile — uses the RNA large dataset from S3.

    Use as follows:
        nextflow run ewels/rustqc-benchmarks -profile test_full,docker

----------------------------------------------------------------------------------------
*/

// Include the RNA large test configuration
includeConfig 'rna_test_full.config'
