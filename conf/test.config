/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running minimal tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Default test profile — uses the RNA small dataset.

    Use as follows:
        nextflow run seqeralabs/rustqc-benchmarks -profile test,docker

----------------------------------------------------------------------------------------
*/

// Include the RNA small test configuration
includeConfig 'rna_test.config'
